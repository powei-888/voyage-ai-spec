# Infra

Infrastructure adapters belong here, including Prisma, object storage, queue
clients, OCR providers, and AI provider interfaces. MVP implementations should
start with mock providers before real external services are connected.
