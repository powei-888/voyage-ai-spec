{
  "name": "voyage-ai",
  "version": "0.2.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "dotenv -- npm run dev --workspace @voyage/web",
    "dev:api": "dotenv -- npm run dev --workspace @voyage/api",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "prisma:generate": "dotenv -- npm run prisma:generate --workspace @voyage/api",
    "prisma:validate": "dotenv -- npm run prisma:validate --workspace @voyage/api",
    "prisma:migrate": "dotenv -- npm run prisma:migrate --workspace @voyage/api",
    "prisma:seed": "dotenv -- npm run prisma:seed --workspace @voyage/api",
    "db:reset": "dotenv -- npm run prisma:migrate --workspace @voyage/api -- reset --force"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "dotenv-cli": "^11.0.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "postcss": "8.5.25",
    "multer": "2.2.0",
    "fast-uri": "3.1.4",
    "find-my-way": "9.7.0",
    "sharp": "0.35.3",
    "brace-expansion": "5.0.9"
  }
}
